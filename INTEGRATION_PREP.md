# 機能統合に向けた準備

現時点では、開催情報管理とモバイル表示管理の機能統合は行わない。
大きな構造変更・保存形式変更・画面統合を避け、まず既存契約と責務境界を固定する。

## 対象

- `plugins/garden-opening-status/`
- `plugins/mobile-layout-manager/`

## 現在の責務

### 開催情報管理

- 春・秋・冬の会期情報
- 公開状態・予約公開
- トップページ開催状況
- SEO / 構造化データ
- 会期ページ表示補正
- Instagram表示
- 管理画面プレビュー

### モバイル表示管理

- スマホ表示全体の補正
- 投稿・固定ページ単位のスマホ表示補正
- トップスライダーのスマホ専用画像
- スマホ専用画像の表示位置
- スマホ実画面プレビュー

## 統合時に引き継ぐ既存契約

### モバイル表示管理

- option: `mlm_options`
- preview draft user meta: `_mlm_preview_draft`
- post meta:
  - `_mlm_enabled`
  - `_mlm_font_scale`
  - `_mlm_heading_scale`
  - `_mlm_side_padding`
  - `_mlm_hide_thumbnail`
- テーマ側PCスライダー参照:
  - option: `dp_options`
  - `slider_image1`
  - `slider_image2`
  - `slider_image3`

### 開催情報管理

- option: `garden_opening_status_options`
- version option: `garden_opening_status_version`
- layout option: `gos_v3_layout_templates`
- default layout option: `gos_v3_default_layout_template`

既存のoption名・投稿メタ名は、統合時に明示的な移行処理を用意するまで変更しない。

## トップスライダーの現行契約

統合準備では、まずトップスライダーだけを独立した契約として扱う。

### PC画像

PC画像の正本はテーマ側の `dp_options`。

```text
dp_options
├─ slider_image1
├─ slider_image2
└─ slider_image3
```

各値はWordPressメディアの attachment ID。
モバイル表示管理は現在、この値を読み取るだけで書き換えていない。

### スマホ画像

スマホ専用画像の正本は `mlm_options`。

```text
mlm_options
├─ top_enabled
├─ breakpoint
└─ slider
   ├─ 1
   │  ├─ image_id
   │  ├─ position_x
   │  └─ position_y
   ├─ 2
   └─ 3
```

- `image_id`: WordPressメディアの attachment ID
- `position_x`: 0〜100%
- `position_y`: 0〜100%
- `top_enabled`: スマホ用スライダー差し替えのON/OFF
- `breakpoint`: スマホ用表示へ切り替える幅

`top_enabled` はモバイル表示全体の `enabled` とは独立して動作する。

### 表示対象スロット

現在の実装では、テーマ側PC画像が設定されているスロットだけが実際のスライダー表示対象になる。

例:

```text
slider_image1 = 設定あり → スライダー1を表示
slider_image2 = 未設定   → スライダー2は表示対象外
slider_image3 = 設定あり → スライダー3を表示
```

スマホ専用画像だけ設定しても、対応するテーマ側PC画像が未設定なら新しいスライドは生成されない。
統合時にもこの仕様を暗黙に変えない。

### 現在のスマホ差し替え経路

モバイル表示管理はトップページで、既存テーマが生成した `#top-slider-item1`〜`3` の背景画像を上書きする。

現在は以下の2経路で差し替えを補強している。

1. `wp_head` 側でスマホ用CSSを出力
2. `wp_footer` 側でCSS + JavaScriptを出力し、テーマ側スライダーの再描画後にも再適用

JavaScript側ではMutationObserverとスライダーイベントを監視して再適用している。
この二重経路は現行テーマでの表示安定化のために存在するので、統合準備段階では整理・削除しない。

## 統合候補

最初の統合対象はトップスライダー管理に限定する。

想定する最終形:

```text
開催情報管理
└─ トップページ管理
   └─ スライダー1〜3
      ├─ PC画像
      ├─ スマホ画像
      └─ スマホ表示位置
```

PC画像は既存テーマの `dp_options` を正本として扱う。
スマホ専用画像は、統合完了までは `mlm_options` を正本として維持する。

## 段階的な移行順序

大きな変更を一度に行わず、次の順序で進める。

### 段階1: 読み取りだけ統一

開催情報管理側から以下を読み取れる状態にする。

- `dp_options` のPC画像1〜3
- `mlm_options` のスマホ画像1〜3
- スマホ画像のX/Y位置
- `top_enabled`
- `breakpoint`

この段階では保存先も公開表示も変更しない。

### 段階2: 管理画面だけ統合

開催情報管理内にトップスライダー管理画面を追加する。
保存先はまだ既存のまま維持する。

```text
PC画像変更      → dp_options
スマホ画像変更  → mlm_options
表示位置変更    → mlm_options
```

旧モバイル表示管理から同じ設定を開いても同じデータが見える状態にする。
公開フックはまだ旧プラグイン側を使用する。

### 段階3: フロント責務を移管

開催情報管理側へスマホ用スライダー差し替え処理を移す。
この時点で二重適用防止の切替条件を実装する。

移管完了までは、同じフロント処理を両プラグインから同時に実行しない。

### 段階4: 旧プラグインを縮小

トップスライダー機能だけを旧モバイル表示管理から停止する。
投稿・固定ページのスマホ補正等はまだ残す。

### 段階5: 残機能を判断

トップスライダー以外のモバイル表示機能を開催情報管理へ統合する必要があるかを別途判断する。
トップスライダー統合を理由に、無関係な投稿・固定ページ補正まで自動的に移さない。

## 二重適用を防ぐ条件

フロント処理移管時には、以下のどちらか一方だけがスライダー差し替えを担当する状態を必須とする。

```text
旧モバイル表示管理が担当
または
開催情報管理が担当
```

移行途中に両方が `#top-slider-item*` を書き換える構成は禁止する。

切替判定は設定値の存在だけで推測せず、移管状態を明示するフラグを用意してから実施する。

## 旧プラグインを外せる条件

トップスライダー機能については、最低限以下が揃ってから旧側を停止する。

- PC画像1〜3が開催情報管理から確認できる
- PC画像1〜3を開催情報管理から安全に変更できる
- スマホ画像1〜3が既存値のまま表示される
- X/Y位置が既存値のまま維持される
- `top_enabled` と `breakpoint` の挙動が維持される
- PC表示が変わらない
- スマホ表示が変わらない
- テーマ側スライダー再描画後もスマホ画像が維持される
- 旧プラグインのトップスライダー処理を停止しても表示が変わらない

## 現段階で行わないこと

- `mlm_options` の移行
- 投稿メタの移行
- 管理画面メニューの統合
- フロントフックの移動
- スライダー描画方式の変更
- テーマの `dp_options` 書き換え実装
- 旧プラグイン停止処理
- 開催情報管理本体の大規模分割
- スライダー枚数の増減
- テーマHTML構造の変更

## 次段階へ進む条件

以下を確認してから実装へ進む。

1. PCスライダーの保存・表示契約を確定
2. スマホ専用画像の既存表示経路を確定
3. 二重適用を防ぐ切替条件を決定
4. `mlm_options` からの移行方法を決定
5. 旧プラグインを停止できる完了条件を決定

1と2は現行コード上の契約を本書へ記録済み。
次は、既存データを変更せずに両設定を読み取る境界を作る。

この準備段階では、既存公開表示と保存データを変更しない。
